# Manual upload folder

Put downloaded CSV files here when authentication or automated ingestion is not working.

Example file name:

```text
hdb_resale_from_data_gov.csv
```

This folder is intentionally kept small in Git. Do not commit very large CSV files unless your assignment requires it.
